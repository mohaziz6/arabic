# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## المشروع

**ديوان التحدي** — لعبة أدبية عربية بين لاعبَين، كل لاعب من جهازه ونافذته الخاصة، والحكم
وكيل ذكاء اصطناعي يسمع أصواتهم، يقيّم الإجابات، ويولّد أسئلة جديدة كل جولة.

**اللعبة الأولى «المحاكمة» مبنية وتعمل** من الطرف للطرف: غرفة بين جهازين، وقاضٍ
يولّد القضية ويقيّم المرافعات وينطق بالحكم. بقية ألعاب الكتالوج تصميم فقط.

## التشغيل

```bash
npm install
npm start                              # الخادم على 8000، يخدم الواجهة أيضاً
npm test                               # ٢٨ اختباراً: آلة الحالات + من الطرف للطرف
node --test server/state.test.js       # ملف واحد
```

بلا `ANTHROPIC_API_KEY` يعمل قاضٍ وهمي يغطي التدفّق كاملاً — فتُختبر اللعبة بلا تكلفة.

## البنية

تطبيق صفحة واحدة بثلاث شاشات في `index.html`، كل واحدة `<section class="screen">`.
التنقل بإضافة/إزالة `is-active` عبر `show(id)` في `assets/app.js` — لا يوجد راوتر ولا تحميل صفحات.

```
server/rules.js       المراحل والبطاقات وشرط الفوز — مشتركة بين الخادم والواجهة
server/state.js       الوكيل الكبير: دوال خالصة بلا شبكة ولا نموذج لغوي
server/judges.js      شخصيات القضاة الثلاثة — نصوصها لا تغادر الخادم
server/judge.js       القاضي: ٣ دوال بمخرج منظّم + قاضٍ وهمي بلا مفتاح
assets/judge-art.js   ظلال القضاة (SVG بلا ملامح وجه)
assets/card-art.js    رسوم الأسلحة: قلم ودواة، نخلة، عود، مطرقة
assets/armory.js      خزانة الأسلحة وإعلان الرمية وأصواتهما
assets/judge-reveal.js انميشن القرعة وصوتها المولّد بـ Web Audio
server/index.js       HTTP + WebSocket، الغرف، تدفّق المحاكمة
assets/trial.js       شاشة المحاكمة — عرض وإدخال فقط
assets/speech.js      transcribe() خلفها Web Speech API
index.html            الشاشات: screen-start → screen-games → screen-lobby/screen-trial
assets/games.js       ثابت GAMES — كتالوج الألعاب (بيانات فقط، بلا منطق)
assets/app.js         كائن state، التنقل، بناء البطاقات من GAMES، ربط الأحداث
assets/background.js  خلفية المعلقات على canvas — مستقلة تماماً عن منطق الواجهة
assets/styles.css     نظام التصميم: متغيرات CSS للألوان والحواف والظلال
assets/fonts.css      تعريفات @font-face المحلية
assets/fonts/         ملفات woff2 (عربي + لاتيني فقط)
```

الملفات محمّلة بوسوم `<script>` عادية بالترتيب — `games.js` قبل `app.js` لأن `GAMES` عالمي.

### إضافة لعبة

عدّل مصفوفة `GAMES` في `assets/games.js` فقط. البطاقة تُبنى تلقائياً في `renderGames()`.
كل عنصر يحتاج: `id`, `name`, `tagline`, `desc`, `glyph` (حرف عربي واحد), `tags`, `heat` (١-٣), `duration`.

بطاقة الاختيار العشوائي (`id: 'random'`) مكتوبة يدوياً في `randomCard()` — ليست ضمن `GAMES`.

**أسماء الألعاب وأوصافها الحالية مبدئية للتقييم** ولم تُعتمد بعد؛ تُبدَّل بحرية.

## قواعد لازمة في هذا المشروع

الصفحة `dir="rtl"` بالكامل، وهذا يفرض أموراً تنكسر بصمت إن أُهملت:

- **رموز الغرف اللاتينية داخل نص عربي** تحتاج `direction: ltr; unicode-bidi: isolate` وإلا انعكس ترتيب حروفها. مطبّق على `.room-chip b` و `.code-display` و `input.code`.
- **لا تستخدم `·` أو `•` فاصلاً** بين نص عربي وأرقام عربية-هندية — محرف محايد يقفز مكانه. استُخدم `،` بدلاً منه.
- **`[hidden]` مُعرَّف صراحة** في `styles.css` بـ `display: none !important`، لأن `.field` عليها `display: block` تتجاوز السلوك الافتراضي للسمة.
- استخدم الخصائص المنطقية (`inset-inline-start`, `margin-inline`) لا `left`/`right`.

## نظام التصميم

**الطابع: صحراوي نجدي فاتح بروح عصرية.** الخلفية `#faf8f4`، البطاقات بيضاء، النص `#1f1b16`،
والتركيز ذهب باهت. الألوان كلها متغيرات في `:root` — لا تكتب لوناً مباشرة في قاعدة.

**الخطوط مستضافة محلياً** في `assets/fonts/` — لا استدعاء لـ Google Fonts وقت التشغيل.
لتحديثها: نزّل CSS الخط بـ `curl` بترويسة User-Agent لمتصفح، واحتفظ بمقاطع `arabic` و `latin` فقط.

- **Aref Ruqaa للعناوين الكبيرة فقط (‏≥30px)**: `.title` و `.section-head h2` و `.lobby h2`.
  دون هذا الحجم يصير مزدحماً وصعب القراءة — جُرِّب على عناوين البطاقات (19px) وفشل.
- **IBM Plex Sans Arabic لكل ما عداه**، بما فيه عناوين البطاقات والحروف المفردة داخل الأقواس
  (حروف Aref Ruqaa المفردة تطلع صغيرة جداً داخل دائرة).

**التباين:** `--gold` (#c08a3e) للحدود والرموز فقط — نسبته على الأبيض ٣٫٢:١ ولا تكفي للنص.
للنص الصغير الذهبي استخدم `--gold-deep` (#9a6b24).

**النقوش النجدية:** `.najdi-band` شريط مثلثات رفيع، يُستخدم كفاصل فقط. والأقواس الطينية
هي `border-radius` غير متماثل على `.seal` و `.glyph` و `.mini-seal`.

**خلفية المعلقات** (`background.js`) لوحان: `#bg` خلف المحتوى للانجراف الهادئ والنفور من
مؤشر الماوس، و `#fg` فوقه (`z-index: 9`) للانفجار عند الأحداث — الانفجار على اللوح الخلفي
يختفي وراء البطاقات البيضاء فلا يُرى. يُستدعى بـ `Diwan.burst(x, y, count)`،
ويحترم `prefers-reduced-motion`.

## الفرع

التطوير على `claude/diwan-challenge-game-design-fq77kt`. رسائل الالتزام بالعربية.

## سير العمل والسكيلز

جلسات الويب تشغّل `.claude/hooks/session-start.sh` تلقائياً: يرفع سيرفر معاينة على
`http://localhost:8000` ويضبط مسار Chromium لـ Playwright. لا يفعل شيئاً محلياً.

السكيلز المعتمدة لهذا المشروع:

- **`/claude-api`** — إلزامي قبل كتابة أي كود يمسّ Claude (الوكيل الحكم، توليد الأسئلة،
  تقييم الإجابات). لا تعتمد على معلومات محفوظة عن الموديلات أو الأسعار.
- **`/run`** — لتشغيل اللعبة وأخذ لقطات والتحقق أن التغيير يعمل فعلاً في المتصفح.
  Chromium مثبّت في `/opt/pw-browsers/chromium`؛ مرّره كـ `executablePath` ولا تنزّل متصفحاً.
- **`/code-review`** قبل كل دفع.
- **`/security-review`** إلزامي عند إدخال مفاتيح API، أو غرف اللعب، أو إذن المايكروفون.

## الخطوة القادمة

**اللعبة الأولى: المحاكمة.** مواصفتها الكاملة في `docs/trial-agent.md` — اقرأها قبل
كتابة أي منطق لعب. القرارات المحسومة: مسار الجلسة، شرط الفوز، مصدر القضية، وبنية الوكيل.

مبدأ معماري لازم: **الوكيل الكبير آلة حالات في الخادم لا نموذج لغوي**، وقاضي المحاكمة
ثلاث دوال بمخرج منظّم لا حلقة أدوات. مخالفة هذا تضاعف التكلفة وتُفقد الحتمية.

قواعد لا تُخالَف في الخادم:

- **المفتاح والبطاقات والدرجات لا تغادر الخادم.** المتصفح يرى `viewFor` فقط،
  وهي تحجب يد الخصم وبطاقته المعلّقة قبل التقييم.
- **`resolvePublic` قائمة بيضاء** لا قائمة سوداء — بدونها يُقدَّم `.git/config` و`.env`.
- **`room.busy` يحرس كل نداء للقاضي.** بدونه ضغطتان تبدآن محاكمتين وتصرفان نداءً مكرراً.
- أي فشل في نداء القاضي يجب أن **يعيد بثّ الحالة**، وإلا بقي اللاعب عالقاً بلا لوحة.
- **نص شخصية القاضي ثابت البايتات** ولا يغادر الخادم. أي متغيّر داخله يُبطل
  التخزين المؤقت صامتاً ويضاعف تكلفة كل نداء.
- `REVEAL_MS` يجب أن يبقى أطول من انميشن القرعة، وإلا ظهرت القضية والستار مرفوع.
- **البطاقات أسلحة تُرمى على المترافع** لا قيوداً على النفس: `canPlayCard` ترفض
  الرمي في دورك. ونقاط القيد تقع على المتحدّث لا على الرامي.
- **القيد المرمي ظاهر للطرفين** في `viewFor` — الهدف يجب أن يراه ليصارعه.

اللعبة التالية تُبنى بنسخ نمط `rules/state/judge`، ومنها يُستخرج المشترك.
