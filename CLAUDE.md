# CLAUDE.md

هذا الملف مرجع Claude Code عند العمل في هذا المستودع. اقرأه كاملاً قبل أي تعديل — فيه
قرارات اتُّخذت عمداً وقد تبدو غريبة بدون سياقها، وقواعد كسرها يُعطّل الشيء بصمت.

## فكرة المشروع

**ديوان التحدي** منصّة ألعاب أدبية عربية لشخصين، كل واحد من جهازه ونافذته (لا يشترط
نفس المكان). من كتالوج ١١ لعبة مخطَّطة، **خمس مبنيّة وتعمل كاملةً من الطرف للطرف**؛
الباقي (٦) بطاقات عرض فقط بلا منطق ولا شاشة (أسماؤها وأوصافها مبدئية، غير معتمدة).

**والألعابُ تُلعب مفردةً أو مجتمعةً في «مباراة»**: عدّة ألعابٍ بالتتابع في ديوانٍ
واحد، فائزُ كلٍّ يأخذ وعاءً ثابتاً، ومن كسب أكثر الجولات كسب المباراة — انظر
قسم «المباراة» أدناه و`docs/match.md`.

### اللعبة ١: المحاكمة (`muhakama`)

لاعبان يتقمصان **مدّعياً** و**مدّعى عليه** أمام قاضٍ وكيل ذكاء اصطناعي (Claude). القاضي
يولّد قضية عبثية مضحكة، يدير ست مراحل (القضية → مرافعة الادعاء → مرافعة الدفاع → ردّ
الادعاء → ردّ الدفاع → الحكم)، يقيّم كل مرافعة صوتية فور انتهائها، وينطق بحكم نهائي.
الفوز بمحاكمتين من أصل ٣ (`WINS_NEEDED=2`, `MAX_TRIALS=3`)، والأدوار تتبدّل من المحاكمة
الثانية. كل لاعب يملك ٤ **بطاقات-أسلحة** (فصحى، مثل، بيت شعري، اعتراض) تُرمى على
**الخصم المتحدّث** لتقييّده أثناء كلامه — وليست قيوداً يفرضها اللاعب على نفسه. قبل بدء
كل محاكمة: انميشن قرعة (روليت) يختار واحداً من ٣ قضاة بشخصيات مختلفة، بصوت مولَّد
بـ Web Audio بلا موسيقى.

### اللعبة ٢: سَنَد (`sanad`)

لاعبان: **راوٍ** و**مُنصِت**. تظهر للراوي فقط شخصية أدبية عربية تاريخية حقيقية (المتنبي،
الجاحظ، الخنساء...) مع سؤال (نشأته/موقف/نهايته) وثلاث روايات: **صادقة** (نقطة واحدة)،
**مختلَقة معقولة** (٣ نقاط)، **مختلَقة عبثية واضحة** (٥ نقاط). الراوي يختار واحدة، يرويها
شفهياً للمنصِت **بمكالمة خارجية بينهما — لا مايكروفون في التطبيق إطلاقاً**، فيتناقشان
٩٠ ثانية، ثم يضغط المنصِت "صادق" أو "كاذب". **المحقّ يأخذ نقاط الخيار** أياً كان (حتى لو
كانت الرواية صادقة والمنصِت كذّبها ظلماً، الراوي يكسب النقطة لأنه صدق). أربع جولات لكل
جلسة (شخصيتان لكل لاعب، بالتبادل الصارم `[a,b,a,b]`). في نهاية الجلسة: مشهد ختامي
(`finale`) يميّز الفائز (ختم ذهبي نابض + أشعة) عن الخاسر (ختم خافت غارق بلا أشعة).

**بلا وكيل ذكاء اصطناعي عمداً** — الحكم بين اللاعبين نفسيهما، فلا نداء ولا تكلفة.

### اللعبة ٣: مَعاني (`maani`)

سباق مفردات في ثلاثة مستويات، **أربعة أسئلة لكل مستوى** (١٢ سؤالاً للجلسة):

1. **المقابلة** (نقطة): كلمتان وثلاثة أزرار — ترادف/تضاد/لا علاقة.
2. **الحصاد** (نقطتان): كلمة وشبكة ١٥ كلمة، والمطلوب عشوائياً مرادفها أو ضدّها.
3. **الدخيل** (٣ نقاط): ٦ كلمات، خمسٌ يجمعها معنًى وسادسةٌ دخيلة.

**سباق متزامن**: السؤال يظهر للخصمين في اللحظة نفسها بالشبكة نفسها والترتيب نفسه
(يُرصف في الخادم)، و**أول من يصيب يأخذ النقاط ويُغلق السؤال** ولو لم يجب خصمه.
ومن أخطأ **خرج من السؤال وحده** وبقي مفتوحاً لخصمه؛ فإن أخطأ الاثنان فلا نقاط.
لكل لاعب إجابة واحدة لا غير. **بلا مؤقّت إطلاقاً** — الجولة تنتهي بالإجابة لا
بانقضاء ثوانٍ (قرار مستخدم صريح). **بلا وكيل وبلا مايكروفون** — بنك كلمات ثابت.
الانتقال بضغطة «التالي» من أيّ الطرفين. الختام يستعمل طبقة `#finale` نفسها التي لسَنَد.

### اللعبة ٤: مَن أنا (`man-ana`)

شخصيةٌ عربيةٌ تاريخية تتكلّم عن نفسها، و**تلميحٌ أوضح كل ١٥ ثانية** (٦ تلميحات،
٣ شخصيات للجلسة). الميدان **مفتوح للخصمين معاً** طوال الوقت، والجواب **كتابةً**.

**قيمة الشخصية تتناقص مع كل تلميح** (٦ ثم ٥ … ثم ١) — وهذه هي اللعبة: أُجازف الآن
بتخمينٍ غامضٍ غالٍ، أم أنتظر فيرخص الثمن ويسبقني خصمي؟ **ومن أخطأ قُفل عليه حتى
التلميح التالي** لا حتى آخر الشخصية، فالخطأ يكلّف وقتاً وفرصة بلا أن يُخرجه.
**والتخمين الخاطئ يُعرض بنصّه للطرفين.** إن نفدت التلميحات بلا إصابة، تُطوى
الشخصية بلا نقاط ويُكشف اسمها. بلا وكيل وبلا مايكروفون.

**هذه أول لعبة فيها إيقاع زمني يقوده الخادم** — انظر قسم المعمارية.

### اللعبة ٥: المزاد (`mazad`)

**لعبةٌ يدويّة** (قرار مستخدم صريح): لا بنك محتوى ولا وكيل ولا «جوابٌ صحيح» يعرفه
الخادم. اللاعبان يكتبان الشرط بأنفسهما («اعطني أسماء من المبشرين بالجنة»)، ويضبطان
الوقت والنقاط، ثم **يتزايدان بالتناوب** على عددٍ يقدران عليه: ٥… ١٠… ١٢… حتى يقول
أحدهما **«عليك — هاتها»**، فتقع على **صاحب آخر مزايدة** بالرقم الذي زايده بنفسه.
يشتغل العدّاد، وتُسجَّل إجاباته **بالمايك أو بالكتابة**، ثم يحكمان بضغطة:
**أتمّها ← له، عجز ← لخصمه**. الجولات **بلا حدّ** — تنتهي الجلسة بضغطة منهما.

**وكلاهما يملك كل الأدوات** (قرار صريح): الشرط والوقت والنقاط والعدّاد والإجابات
والحكم — كلها بيد الاثنين. **الاستثناء الوحيد دور المزايدة**، فهو تناوبٌ بطبيعته
ولولاه لم يكن مزاداً. وهذه **اللعبة الثانية التي تستعمل المايكروفون** (بعد المحاكمة)،
لكنه هنا **مسجِّل لا محلِّل**: كل مقطعٍ نهائي يصير شارةً تُحذف بضغطة.

**وهي الاستثناء الوحيد لقاعدة `viewFor` الحاجبة** — انظر المعمارية.

### المباراة: جولاتٌ من ألعابٍ مختلفة (`match.js`)

طبقةٌ فوق الألعاب الخمس لا لعبةٌ سادسة. تُختار عدّة ألعاب (حتى `MAX_GAMES`=٥)
فتصير جولاتٍ في ديوانٍ واحد، و**فائز كل لعبة يأخذ ٣ نقاط مباراة والتعادل نقطةٌ
لكلٍّ** (`POT_WIN`/`POT_TIE`).

**ولا تُجمَع نقاط الألعاب الخام أبداً** (قرار مستخدم صريح): سلالمها متباعدة —
مَعاني حتى ٢٤، ومَن أنا ١٨، وسَنَد نحو ١٢، والمزاد بلا سقف، والمحاكمة أحكامٌ لا
نقاط — فالجمع الخام يجعل لعبةً واحدة تبتلع المباراة. وبالوعاء الثابت يستوي من
سحق خصمه ٢٤ إلى صفر ومن فاز بفارق نقطة: المباراة تسأل «كم لعبةً كسبت».

**والجولة بلعبتها كاملةً بلا تقصير** (قرار صريح)، والتقدّم بضغطة «الجولة التالية»
من أيّ الطرفين بعد أن تُرى النتيجة.

## التقنيات والخدمات

- **Node.js ≥20**، وحدات ES (`"type":"module"`)، بلا إطار عمل وبلا خطوة بناء (build step).
  خادم HTTP يدوي + [`ws`](https://github.com/websockets/ws) للـ WebSocket، بلا Express.
- **`@anthropic-ai/sdk` (^0.72.0)** — قاضي المحاكمة فقط. `client.messages.parse()` مع
  **Zod (^3.24.1)** عبر `zodOutputFormat()` لمخرجات منظّمة (structured output)، لا حلقة
  أدوات (tool loop). النموذج `claude-opus-5`. `output_config.effort` يُضبط لكل نداء
  (`low` لتقييم مرافعة، `medium` لتوليد قضية، `high` للحكم النهائي) لخفض التكلفة.
  تخزين مؤقت (`cache_control:{type:'ephemeral'}`) على نص شخصية القاضي.
  **راجع سكيل `/claude-api` قبل أي تعديل على نداءات النموذج — لا تعتمد على معلومات
  محفوظة عن الموديلات أو الأسعار.**
- **بلا مفتاح (`ANTHROPIC_API_KEY`) يعمل قاضٍ وهمي (mock)** يحاكي شخصية كل قاضٍ حتى بلا
  نداء فعلي — اللعبة تُختبر وتُلعب بالكامل بلا تكلفة ولا مفتاح.
- **`selfsigned` (^3.0.1)** — شهادة HTTPS ذاتية التوقيع مخزَّنة في `.cert/` (مُستبعدة من
  git)، لازمة لأن المتصفح يمنع المايكروفون على `http://` غير `localhost`.
  ⚠️ **`generate()` في النسخة المثبَّتة تُرجع Promise ويجب انتظاره بـ `await`** — فخ توافق
  اكتُشف أثناء التطوير، والاستيراد ديناميكي (`await import('selfsigned')`).
- **Web Speech API** (`SpeechRecognition`/`SpeechSynthesis`, متصفحات Chromium فقط — لا
  Firefox) — تسجيل/نطق صوت المحاكمة فقط. **بقية الألعاب الثلاث لا تستخدم المايكروفون إطلاقاً.**
- **Web Audio API خام** (oscillators + noise buffers يدوية) — كل المؤثرات الصوتية في
  الألعاب الخمس (نقرات الروليت، المطرقة، رمي البطاقة، رصف الكلمات، لافتة المستوى،
  وصول التلميح ونقرة العدّ الأخيرة، قفزة المزايدة وجرس الصفر وشارة الإجابة، عدّ
  النقاط). **لا ملفات صوت، لا موسيقى** في أي مكان بالمشروع — قرار متعمَّد.
- **`node --test`** المدمج في Node — بلا Jest ولا أي إطار اختبار خارجي.
- **Playwright** لأخذ لقطات والتحقق البصري أثناء التطوير (ليس جزءاً من `npm test`).
  Chromium مثبَّت مسبقاً على `/opt/pw-browsers/chromium` — مرّره `executablePath` **ولا
  تحاول تثبيت متصفح** (`playwright install` سيفشل/غير لازم). راجع سكيل `/run`.

## التشغيل

```bash
npm install
npm start                              # الخادم على 8000، يخدم الواجهة أيضاً
npm run https                          # HTTPS على 8443 — لازم لتفعيل المايكروفون بالمحاكمة
npm test                               # node --test server/*.test.js — 136 اختباراً، كلها تمر حالياً
node --test server/sanad.test.js       # ملف واحد
```

جلسات الويب (claude.ai/code) تشغّل `.claude/hooks/session-start.sh` تلقائياً: سيرفر
معاينة على `http://localhost:8000` ويضبط مسار Chromium — لا تفعل شيئاً يدوياً محلياً.

بلا `ANTHROPIC_API_KEY` في `.env` يعمل قاضي المحاكمة الوهمي — **لم يُختبر القاضي الحقيقي
بمفتاح فعلي في هذه البيئة إطلاقاً طوال التطوير**، فجودة أحكامه الفعلية غير مُتحقَّق منها.

## بنية المشروع

تطبيق صفحة واحدة (SPA) بثماني شاشات (`<section class="screen">`) في `index.html`:
`screen-start` → `screen-games` → `screen-lobby` → (`screen-trial` أو `screen-sanad`
أو `screen-maani` أو `screen-man-ana` أو `screen-mazad`)، زائد طبقات (`<div>` خارج
`.app`): `judge-draw`، `finale` (مشتركة بين سَنَد ومَعاني ومَن أنا والمزاد)،
`mn-banner`، `armory`، `strike`، `gavel-scene`، و`session-panel` (لوحة الجلسة)
مع زرّها الجانبي `session-btn`.
التنقل بإضافة/إزالة `is-active` عبر `show(id)` في `assets/app.js` — **لا راوتر ولا تحميل
صفحات**. الملفات محمَّلة بوسوم `<script>` عادية بالترتيب (`games.js` قبل `app.js` لأن
`GAMES` عالمي)، وبعضها `type="module"` (`app.js`, `trial.js`, `sanad.js`, `maani.js`,
`man-ana.js`, `mazad.js` واستيراداتها).

### الخادم (`server/`)

| الملف | الوظيفة |
|---|---|
| `index.js` | نقطة الدخول: HTTP+WS، خريطة الغرف، `resolvePublic()` (قائمة بيضاء للملفات الثابتة)، حدّ المعدّل لكل مقبس (`RATE`)، `ENGINES`/`GAME_LABELS`/`GAME_MESSAGES` (حرس عزل الألعاب الخمس)، `scheduleClue`/`clearClue` (مؤقّت تلميح «مَن أنا»)، `scheduleBell`/`clearBell` (جرس صفر «المزاد») و`applyMazad`، طبقة المباراة (`switchGame`/`settleRound`/`gameEnded`/`match-next`) ومهلة الإمهال (`REAP_MS`/`keepAlive`/`scheduleReap`)، تدفّق المحاكمة الكامل (`beginTrial`/`handleSpeech`/`handleObjection`/`concludeTrial`)، مناولات سَنَد (`sanad-*`) ومَعاني (`maani-*`) ومَن أنا (`man-ana-*`) والمزاد (`mazad-*`)، انضمام/إعادة اتصال، `--https`/`HTTPS=1`. |
| `state.js` | آلة حالات المحاكمة الخالصة — **بلا شبكة ولا نموذج لغوي**: `createSession`, `startTrial`, `canPlayCard`, `playCard`, `submitSpeech` (لا ينتظر الحكم), `applyJudgement`, `recordVerdict`, `viewFor` (حجب الخصوصية). |
| `rules.js` | ثوابت المحاكمة: `PHASES` الست، `CARDS` الأربع (أسلحة)، `WINS_NEEDED`, `MAX_TRIALS`. |
| `judges.js` | شخصيات القضاة الثلاث (الميزان/الريح/العُرف) — **نص `persona` ثابت البايتات ولا يغادر الخادم**؛ `publicJudges()` يحذفه قبل الإرسال للمتصفح. |
| `judge.js` | ٣ دوال قاضٍ بمخرج منظّم (`generateCase`, `judgeSpeech`, `judgeObjection`, `deliverVerdict`) + قاضٍ وهمي كامل بلا مفتاح (`hasCredentials()`). |
| `sanad.js` | آلة حالات سَنَد الخالصة — **بلا وكيل**؛ فيها **تحقّق وقت-التحميل** يوقف الخادم فوراً إن كان أي `hint` ناقصاً أو غير ٤ كلمات أو مقتطعاً من نص روايته. `choose`, `rule` (منطق النقاط الست حالات), `next`, `viewFor`. |
| `maani.js` | آلة حالات مَعاني الخالصة — **بلا وكيل**؛ فيها **تحقّق وقت-التحميل** يوقف الخادم إن اختلّ بنك الكلمات. `buildDeck` (١٢ سؤالاً مبنيّة مقدماً)، `answer` (منطق السباق)، `next`, `viewFor`. |
| `man-ana.js` | آلة حالات «مَن أنا» الخالصة — **بلا وكيل وبلا مؤقّت** (المؤقّت في `index.js` فتبقى هذه خالصةً تُختبر بلا انتظار): `startSession`, `guess`, `advanceClue`, `next`, `viewFor`، و`normalize`/`matches` لتطبيع الجواب العربي، وتحقّق وقت-التحميل. |
| `match.js` | طبقة «المباراة» الخالصة — **لا تعرف شيئاً عن الألعاب نفسها**، تأخذ نتيجةً (`winnerId` أو `null`) وتُخرج أرصدة: `createMatch`, `recordRound` (لا تصرف وعاء الجولة مرّتين), `advance`, `isLastRound`, `leader`, `seat`, `viewFor`. |
| `mazad.js` | آلة حالات «المزاد» الخالصة — **بلا وكيل وبلا بنك محتوى وبلا سرّ**: `configure` (تحقّقٌ كاملٌ قبل أي تعديل), `openBidding`, `raise`, `handOver`, `addAnswer`, `removeAnswer` (بختم النصّ), `judge`, `nextRound`, `endSession`, و**المؤقّت بيانات لا `setTimeout`**: `timeLeft(s, now)` دالّة خالصة، و`freeze` قبل كل ما يوقفه. `viewFor` متماثلة عمداً. |
| `man-ana-figures.js` | ١٩ شخصية × ٦ تلميحات + `aliases` (صيغ الجواب المقبولة) و`reveal`. **الاسم والصيغ والكشف لا تغادر الخادم قبل الحلّ، ولا التلميحات التي لم يحن وقتها.** |
| `maani-words.js` | بنك الكلمات: ٥٤ زوجاً، ٣٤ حصاداً، ٢٨ دخيلاً، ٦٤ كلمة حشو. **الجواب لا يغادر الخادم قبل الكشف.** |
| `sanad-figures.js` | ١٧ شخصية × ٣ أسئلة × ٣ خيارات = ١٥٣ خياراً، كل خيار `{kind, hint, text}`. **لا يغادر الخادم أبداً بشكل كامل** — المنصِت يرى `hint` فقط قبل الكشف. |
| `setup.js` | `loadEnv()` (لا يكتب فوق متغيرات بيئة موجودة), `lanAddress()`, `ensureCert()` (شهادة HTTPS مخزَّنة مؤقتاً). |
| `*.test.js` | اختبارات `node --test`: `state`, `judges`, `sanad-state`, `sanad`, `maani`, `man-ana`, `mazad`, `match` (وحدات) + `e2e` (يشغّل خادماً حقيقياً بعملية فرعية ويتصل بعملاء WebSocket حقيقيين). |

### الواجهة (`assets/`)

| الملف | الوظيفة |
|---|---|
| `app.js` | كائن `state` عام، `show()`, `renderGames()` من `GAMES`, `burstFrom()`, **`enterGame(id)` واحدة لكل الألعاب** (خريطة `GAME_UI`، بحارس `live` لمنع مقبس مكرَّر وحارس `bound` لمنع ربط مكرَّر), `toggleGame`/`paintPicks` (الاختيار التراكمي), `paintMatch`/`paintFinaleMatch` (شريط المباراة وذيل الختام), `paintSession`/`toggleSession` (لوحة الجلسة), `onState` (تُستدعى من كل وحدة عند كل بثّ), `goHome`, `closeFinale`, `watchFinale`, توجيه رابط الدعوة (`?code=&game=`). |
| `games.js` | مصفوفة `GAMES` — كتالوج الألعاب (بيانات فقط بلا منطق). خمس مبنيّة (`muhakama`, `sanad`, `maani`, `man-ana`, `mazad`)؛ الباقي بطاقات عرض. |
| `trial.js` | شاشة المحاكمة: `connect()`, `render()`, المؤقّت, `submitSpeech`, سجلّ القاضي, `throwState()`/`renderImposed` (حالة رمي الأسلحة). |
| `sanad.js` | شاشة سَنَد (أكثر الملفات تعديلاً): `connect()`, `render()`→`renderStage`/`renderPhase`/`renderReveal`/`renderOver`, `dealOptions()`, `renderTold()` (تلميح محجوب/رواية كاملة), `paintScores()`/`flyThenCount()`/`countUp()` (مع `shownScores`, إلغاء آمن للانميشن الجاري، `prefers-reduced-motion`), `playFinale()`. |
| `maani.js` | شاشة مَعاني: `connect()`, `render()`→`renderBoard`/`renderPair`/`renderHunt`/`renderIntruder`/`renderStatuses`/`renderVerdict`, `maybeAnnounceLevel()` (لافتة المستوى), `paintScores()`/`flyThenCount()`/`countUp()`, `playFinale()`. |
| `man-ana.js` | شاشة «مَن أنا»: `connect()`, `render()`→`renderLadder`/`renderClues`/`renderMisses`/`renderAnswerBar`/`renderReveal`, `dipEmblem()`, عدّاد التلميح المحلي، `paintScores()`, `playFinale()`. |
| `mazad.js` | شاشة «المزاد»: `connect()`, `render()`→`renderTimer`/`renderSetup`/`renderBidding`/`renderChallenge`/`renderAnswers`/`renderJudged`, `showNote()` (شرح الرفض بلا حجب), `startMic()` (كل مقطعٍ نهائي شارة), `setField()` (لا يكتب فوق حقلٍ يُكتب فيه الآن), العدّ المحلي بـ `setInterval`, `paintScores()`/`playFinale()`. |
| `man-ana-art.js` | `emblemSvg()` — قوسٌ طينيّ نجديّ وأربع مخطوطات، SVG مولَّد بمتغيّرات الصفحة. |
| `speech.js` | غلاف Web Speech API: `startListening`/`speak`، إعادة تشغيل تلقائية عند `onend` غير المتعمَّد. |
| `judge-art.js` | `judgeSilhouette(id)` — ظلال SVG للقضاة الثلاثة، بلا ملامح وجه. |
| `judge-reveal.js` | `revealJudge()` انميشن القرعة/الروليت + `announceVerdict()` مشهد المطرقة. |
| `card-art.js` | `cardArt(id)` رسوم SVG للأسلحة الأربعة (قلم ودواة، نخلة، عود، مطرقة). |
| `armory.js` | فتح/غلق خزانة الأسلحة، `showStrike()` إعلان الرمية. |
| `background.js` | خلفية المعلقات المتطايرة (canvas مزدوج: `#bg` خلف المحتوى، `#fg` فوقه للانفجارات). `Diwan.burst(x,y,count)` API عام، يحترم `prefers-reduced-motion`. |
| `styles.css` | نظام تصميم واحد بمتغيرات CSS. |
| `fonts.css` / `fonts/` | خطوط مستضافة محلياً (woff2، عربي+لاتيني فقط). |

### التوثيق (`docs/`)

- `docs/trial-agent.md` — مواصفة كاملة للمحاكمة (سبق بناؤها بالكامل — اقرأها قبل تعديل منطق اللعبة).
- `docs/sanad.md` — مواصفة كاملة لسَنَد (نفس الشيء).
- `docs/maani.md` — مواصفة كاملة لمَعاني (نفس الشيء).
- `docs/man-ana.md` — مواصفة كاملة لمَن أنا (نفس الشيء).
- `docs/mazad.md` — مواصفة كاملة للمزاد (نفس الشيء).
- `docs/match.md` — مواصفة طبقة المباراة (الوعاء الثابت، تبديل الجولة، لوحة الجلسة).

### إضافة لعبة جديدة للكتالوج (عرض فقط، بلا منطق)

عدّل مصفوفة `GAMES` في `assets/games.js` فقط. البطاقة تُبنى تلقائياً في `renderGames()`.
كل عنصر يحتاج: `id`, `name`, `tagline`, `desc`, `glyph` (حرف عربي واحد), `tags`, `heat`
(١-٣), `duration`. بطاقة الاختيار العشوائي (`id:'random'`) مكتوبة يدوياً في `randomCard()`
— ليست ضمن `GAMES`. **أسماء الألعاب وأوصافها الحالية مبدئية للتقييم** ولم تُعتمد بعد.

### بناء لعبة سادسة كاملة

انسخ نمط آلة الحالات الخالصة (`sanad.js` أو `maani.js` أقرب مثالين؛ و`rules.js` +
`state.js` + `judge.js` إن احتاجت وكيلاً)، ثم سجّل رسائلها في `GAME_MESSAGES`
و`ENGINES` و`GAME_LABELS` في `index.js`، وأضف `enterX` إلى خريطة `ENTER` في `app.js`.

**التجريد صار مستحقاً ومتأخراً بعد اكتمال خمسة أمثلة** (كان مؤجَّلاً عمداً على
مثالين). المتكرّر فعلياً بين `assets/sanad.js` و`maani.js` و`man-ana.js` و`mazad.js`:
`esc`, `reveal`/`hide`, `arNum`, `points`, `tone`/`sfx`, `reducedMotion`,
`paintScores`/`flyThenCount`/`countUp`/`shownScores`, و`playFinale` بطبقته المشتركة.
استخراجها إلى وحدة مشتركة (مثلاً `assets/duel.js`) خطوةٌ صحيحة الآن — لكنها **لم
تُنفَّذ بعد**، فالملفات الأربعة مستقلة اليوم وتعديل أحدها لا يمسّ الآخر.
أما آلات الحالات في الخادم فتبقى منفصلة: اختلافها حقيقي لا تكرار.

## المعمارية

- **الوكيل الكبير آلة حالات في الخادم لا نموذج لغوي**: `state.js` و`sanad.js` دوال
  خالصة بلا شبكة ولا LLM — يقرران من يتكلم، من يفوز، ما المسموح. القاضي (Claude) يُستدعى
  فقط لثلاث مهام محدَّدة بمخرج منظّم (توليد قضية، تقييم مرافعة، حكم نهائي) — **ليس حلقة
  أدوات (agentic tool loop)**. مخالفة هذا تضاعف التكلفة وتُفقد الحتمية.
- **خصوصية عبر `viewFor(state, viewerId)` لا عبر CSS**: كل مسار بث للحالة يبني نسخة
  خاصة بكل مشاهِد — يد الخصم في المحاكمة، ونص/نوع/نقاط الرواية غير المكشوفة في سَنَد،
  ونص شخصية القاضي — **لا تُسلسَل (serialize) إلى JSON مرسل للطرف غير المخوَّل أصلاً**،
  لأن أي حجب بـ CSS فقط يُقرأ من أدوات المطوّر في ثوانٍ.
- **`viewFor` المتماثلة استثناءٌ مشروط بانعدام السرّ** (المزاد وحده): حين يكتب
  اللاعبان المحتوى بأنفسهما لا يبقى شيءٌ يُحجَب، فتميّز `viewFor` «أنا» من «خصمي»
  لا غير. **الشرط أن الخادم لا يعرف شيئاً لا يعرفانه** — أي لعبةٍ فيها بنكُ محتوى
  أو جوابٌ أو وكيل تعود إلى الحجب الصارم بلا نقاش.
- **`room.game` لعبةُ الجولة الجارية لا لعبةُ الغرفة الأبدية** (جديد مع المباراة):
  الغرفة تبقى تحمل **لعبةً واحدة في كل لحظة**، فيبقى حارس `GAME_MESSAGES` كما هو
  بلا إضعاف ويتحرّك معها — رسالةُ لعبةٍ انقضت جولتُها تُرفض كما تُرفض رسالةُ لعبةٍ
  لم تُختَر أصلاً. و`switchGame` يبني جلسةً نظيفة **بمعرّفات اللاعبَين نفسها**.
- **`settleRound` يعيش في `broadcast`**: هو المخنق الذي تمرّ منه كل حالةٍ تصل
  اللاعب، فيُصرف وعاء الجولة قبل أن تُرى نتيجتها، و`recordRound` لا تصرفه مرّتين.
  ⚠️ و**آلات الألعاب لا تتّفق على اسم حالة الانتهاء**: المحاكمة `session-over`
  والأربعُ `over`، فـ`gameEnded()` تقبل الاسمين — واعتمادُ اسمٍ واحد كان يجعل
  جولةَ محاكمةٍ لا تُحسم أبداً فتموت المباراة عندها.
- **`REAP_MS` مهلةُ إمهالٍ قبل حذف غرفةٍ خلت**: تبديل الجولة يُغلق مقبسَي الطرفين
  معاً ثم يعيدهما بعد أجزاءٍ من الثانية، وحذفُ الغرفة لحظة خلوّها كان **يُبخّر
  المباراة وأرصدتها**. و`keepAlive` **يعيد مؤقّتات الغرفة** عند العودة (تلميح
  «مَن أنا»، جرس «المزاد») لأن `scheduleReap` أطفأها — ولولا ذلك لعادا إلى «مَن
  أنا» وعدّادُها واقفٌ على صفرٍ إلى الأبد.
- **العودة بمعرّف اللاعب لا بالتقاط أيّ مقعدٍ شاغر**: الطرفان يعودان في اللحظة
  نفسها عند تبديل الجولة، فالالتقاط بالشغور وحده كان يخلط مقعديهما. ومقبسٌ قديم
  لم يُنظَّف بعد يُغلَق ويُخلى لصاحبه بدل أن يدفعه إلى مقعد خصمه.
- **عزل الغرف حسب اللعبة**: كل غرفة تحمل `room.game` (`'muhakama'`|`'sanad'`|`'maani'`|`'man-ana'`|`'mazad'`)،
  و`GAME_MESSAGES` في `index.js` تُسمّي رسائل كل لعبة، و`GAME_OF_MESSAGE` تُشتقّ منها
  فتُرفض أي رسالة جاءت لغير لعبة غرفتها **قبل** أي مناولة، ومناولة `join` ترفض تعارض
  اللعبة. **بلا هذا الحارس رسالة محاكمة ضالّة تُدمّر حالة غرفة سَنَد** (باگ حرج وقع
  فعلياً وأُصلح — انظر الأخطاء أدناه). كان الحارس قائمتين ثابتتين
  (`TRIAL_ONLY`/`SANAD_ONLY`) واستُبدل بخريطة عامّة عند إضافة اللعبة الثالثة، لأن
  القائمتين لا تتوسّعان لثلاث ألعاب بلا خطأ صامت. **أي لعبة جديدة تُسجَّل هنا أولاً.**
- **`resolvePublic()` قائمة بيضاء لا سوداء** للملفات الثابتة المُقدَّمة — بدونها يُقدَّم
  `.git/config` و`.env` لأي زائر (ثغرة حقيقية وُجدت وأُصلحت).
- **`room.busy` يحرس كل نداء للقاضي/مناولة حالة حرجة** — بدونه ضغطتان متزامنتان تبدآن
  محاكمتين وتصرفان نداءً مدفوعاً مكرَّراً.
- **أي فشل في نداء القاضي يجب أن يعيد بثّ الحالة** (`room.busy=false` + `broadcast()` +
  رسالة خطأ للاعب) — وإلا بقي اللاعب عالقاً بلا لوحة لعب بلا أي مخرج.
- **`submitSpeech` لا تنتظر القاضي**: تسجّل المرافعة وتُقدِّم الدور فوراً
  (`judgement:null`)، والتقييم يجري بالتوازي (`Promise` غير مُنتظَر عند الاستدعاء) ثم
  `applyJudgement()` تُلحق الدرجة عند وصولها. `concludeTrial()` تنتظر
  `Promise.allSettled(room.judging)` قبل طلب الحكم النهائي، لضمان تسجيل كل النص أولاً.
  **الانتظار المتزامن كان يجمّد اللعبة أربع مرات في كل محاكمة** (نداء القاضي 5-15 ثانية).
- **الطبقات (overlays) تُفتح بإعادة تدفّق قسرية لا بـ `requestAnimationFrame`**:
  `el.hidden=false; void el.offsetWidth; el.classList.add('is-open')`. الأخير **لا يعمل
  في تبويب خلفي (غير مُركَّز)**، فتبقى شاشة اللاعب غير النشط شفافة إلى الأبد — باگ حرج
  وقع فعلياً واكتُشف فقط عبر لقطات Playwright (الاختبارات الآلية لا تكتشفه). القاعدة
  تنطبق على كل الطبقات: `judge-draw`, `armory`, `strike`, `gavel-scene`, `finale`,
  `mn-banner`.
- **الطبقة التي تغطي لوحاً حيّاً تأخذ `pointer-events:none`**: لافتة مستوى مَعاني
  تعبر فوق سؤالٍ **بدأ سباقه فعلاً**، فابتلاعها النقر كان يمنح من قلّل الحركة سبقاً
  (لافتته تختفي بعد ٩٠٠ms لا ١٧٠٠) في ثلاثة أسئلة من كل جلسة.
- **الإيقاع الزمني يقوده الخادم لا المتصفح** (جديد مع «مَن أنا»): `scheduleClue` في
  `index.js` تكشف التلميح كل `CLUE_MS` وتبثّه للطرفين معاً — لو عدّ كل جهازٍ لنفسه
  لانحرف عن الآخر فرأى أحدهما تلميحاً قبل خصمه، وهو سباقٌ على النقطة نفسها. كل بثّ
  يحمل `clueMsLeft` فيعدّ المتصفح محلياً بينه وبين التالي. وثلاث قواعد لازمة:
  **(١)** تُجدوَل الدورة الجديدة **قبل** البثّ لا بعده، وإلا حمل البثّ ما تبقّى من
  الدورة المنقضية (صفراً) فجلس العدّاد على ٠ دورةً كاملة (باگ وقع وأمسكه اختبار).
  **(٢)** يُلغى المؤقّت عند الحلّ وعند إغلاق الغرفة — مؤقّتٌ على غرفةٍ ميتة يبقى
  يعمل ويمنع خروج العملية. **(٣)** المناولة تتحقّق أن الغرفة ما زالت في `rooms`.
  و**المؤقّت يعيش في الغرفة لا في آلة الحالات**، فتبقى الأخيرة خالصةً تُختبر بلا
  انتظار ثانيةٍ واحدة.
- **والمؤقّت في «المزاد» بياناتٌ لا `setTimeout`**: الحالة تحمل
  `{running, leftMs, startedAt}` و`timeLeft(s, now)` دالّة خالصة، فتُختبر «دقيقةٌ
  مضت» بسطرٍ واحدٍ بلا انتظار. والجدولة الفعلية في الغرفة (`scheduleBell`) بقواعد
  «مَن أنا» الثلاث نفسها، وزيادةً عليها: **يُجمَّد العدّاد قبل بثّ الجرس**
  (`MZ.timerPause`) وإلا بُثّت حالةٌ تقول `running` ورصيدُها صفر، فيقرأ الزرُّ
  «⏸ أوقف» فوق ساعةٍ واقفةٍ على `0:00`. **والجرس يُعلن ولا يُنهي الجولة** — اللعبة
  يدويّة، فالقرار لهما.
- **مطابقة الجواب العربي احتواءٌ لا تساوٍ** (`man-ana.js`): يُجرَّد التشكيل وتُوحَّد
  الألف والياء والتاء المربوطة، وتُقبل أداة التعريف وتُترك. ومن كتب «الشاعر المتنبي»
  فقد عرفه — رفضُه يقفل عليه ١٥ ثانية ويبدو عطلاً لا لعبة. والعكس مرفوض: **جزءُ
  الاسم ليس الاسم**، فـ«سينا» وحدها ليست «ابن سينا».
- **رفض الخادم الناتج عن سباق ليس عطلاً — لكنه ليس صمتاً أيضاً**: رسائل `error`
  من مناولات مَعاني والمزاد تحمل `soft:true` فلا `alert` حاجباً. لكن الابتلاع
  الكامل يصحّ في **مَعاني** وحدها، لأن الحالة الجديدة تصل بعدها بلحظة وتشرح نفسها.
  أما **المزاد** فرفضُه قد لا يتبعه بثٌّ أصلاً (لم يتغيّر شيء)، ولعبةٌ كلّ أدواتها
  بيد الاثنين لا يجوز أن تبتلع ضغطةً بلا خبر — فيُعرض السببُ في `#mz-note`، شارةٌ
  عائمة `pointer-events:none` تختفي وحدها. **وما يمكن منعُه قبل الرفض يُمنَع**:
  زرّ تشغيل العدّاد مُعطَّل خارج طور التحدي، لأن `timerStart` لا يعمل إلا فيه.

## قواعد RTL لازمة (تنكسر بصمت إن أُهملت)

الصفحة `dir="rtl"` بالكامل:

- **رموز الغرف اللاتينية داخل نص عربي** تحتاج `direction:ltr; unicode-bidi:isolate` وإلا
  انعكس ترتيب حروفها. مطبَّق على `.room-chip b`, `.code-display`, `input.code`.
- **لا تستخدم `·`/`•` فاصلاً** بين نص عربي وأرقام — محرف محايد يقفز مكانه. استخدم `،`.
- **أرقام الدرجات/النقاط لاتينية لا عربية-هندية**: صفر عربي-هندي (`٠`) يبدو نقطة مكسورة
  صغيرة في لوحة النقاط — أرقام النقاط `direction:ltr;unicode-bidi:isolate` بلاتيني، بينما
  مؤشرات التقدّم ("١ من ٤") تبقى عربية-هندية. **والمعيار دورُ الرقم لا موضعُه**: رقم
  مزايدة «المزاد» لاتينيٌّ كذلك (`.mz-bid-num`, `.mz-bid-amt`) لأنه رقمٌ يُلتقط تحت
  الضغط — و«٥» عربيّاً-هنديّاً حلقةٌ تُقرأ صفراً، وهذا رقم اللعبة كلِّها.
- **`[hidden]` مُعرَّف صراحة** بـ `display:none!important` في `styles.css`، لأن
  `.field{display:block}` يتجاوز سلوك السمة الافتراضي.
- استخدم الخصائص المنطقية (`inset-inline-start`, `margin-inline`) لا `left`/`right`.

## نظام التصميم

**الطابع: صحراوي نجدي فاتح بروح عصرية.** خلفية `#faf8f4`، بطاقات بيضاء، نص `#1f1b16`،
تركيز ذهب باهت. كل الألوان متغيّرات `:root` — **لا لون مباشر في أي قاعدة CSS**.

**خطوط مستضافة محلياً** في `assets/fonts/` — لا Google Fonts وقت التشغيل. للتحديث: نزّل
CSS الخط بـ `curl` بترويسة User-Agent متصفح، واحتفظ بمقاطع `arabic`/`latin` فقط.

- **Aref Ruqaa لعناوين المشاهد الكبيرة وحدها — ثمانية مواضع لا غير**:
  `.title`/`.section-head h2`/`.lobby h2` (المجموعة نفسها)، `.judge-draw-box h2`،
  `.armory-box h2`، `.gavel-stamp`، `.sanad-over h2`، `.finale-box h2`،
  `.mn-banner-box h2`، `.ma-emblem-title`. كلها **عناوين من كلمتين أو ثلاث** تُلمح لحظةً واحدة، فهي زخرفٌ
  يحمل هوية الديوان لا نصٌّ يُلتقط. (أحجامها `clamp` تنزل إلى ٢٤-٢٨px على الشاشات
  الضيّقة — والمعيار **قِصر العنوان ودورُه**، لا الحجم وحده.)
- **IBM Plex Sans Arabic لكل ما عداه بلا استثناء**: كلمات اللعب، أسماء الشخصيات
  والقضاة، الأسئلة، الأزرار، الأرقام، الفقرات. الوزن (600/700) هو ما يميّز العنوان
  لا الخط.
- **قاعدة الفصل**: هل يُقرأ النص تحت ضغط (سباق، قرار، اسم يُحفظ)؟ إذن Plex.
  هل يُلمح لحظةً كعنوان مشهد؟ عندها فقط Ruqaa.

**التباين:** `--gold` (#c08a3e) للحدود والرموز فقط — نسبته على الأبيض ٣٫٢:١ لا تكفي للنص.
للنص الصغير الذهبي استخدم `--gold-deep` (#9a6b24).

**النقوش النجدية:** `.najdi-band` شريط مثلثات رفيع، فاصل فقط. الأقواس الطينية
`border-radius` غير متماثل على `.seal`, `.glyph`, `.mini-seal`.

**خلفية المعلقات** (`background.js`): لوحان `#bg` (خلف المحتوى، انجراف هادئ ونفور من
الماوس) و`#fg` (`z-index:9`، فوق المحتوى، للانفجار عند الأحداث — انفجار اللوح الخلفي
يختفي وراء البطاقات البيضاء فلا يُرى). `Diwan.burst(x,y,count)`، يحترم `prefers-reduced-motion`.

**بلا ملفات صوت وبلا موسيقى في أي مكان** — كل الأصوات Web Audio مولَّدة برمجياً.

## سكيلز إلزامية لهذا المشروع

- **`/claude-api`** — إلزامي قبل كتابة أي كود يمسّ Claude (القاضي، توليد الأسئلة،
  التقييم). لا تعتمد على معلومات محفوظة عن الموديلات أو الأسعار.
- **`/run`** — لتشغيل اللعبة، أخذ لقطات، والتحقق أن التغيير يعمل فعلاً بالمتصفح (وليس
  فقط أن الاختبارات الآلية تمر — الاختبارات لا تكتشف باگات بصرية/تفاعلية مثل مشكلة
  `requestAnimationFrame` بالتبويب الخلفي).
- **`/code-review`** قبل كل دفع (push). **كل ملاحظة من كل جولة مراجعة عُولجت فعلياً طوال
  هذا المشروع، ولم تُرفض أي ملاحظة كغير مهمة** — التزم بهذا النمط.
- **`/security-review`** إلزامي عند إدخال مفاتيح API، أو غرف اللعب، أو إذن المايكروفون.
- **`/sync-docs`** (أمر المشروع، في `.claude/commands/sync-docs.md`) — يحدّث هذا الملف
  و`README.md` و`docs/` ليطابق الكود الفعلي، ثم يلتزم ويدفع. شغّله في نهاية كل ميزة.

### حارس انحراف التوثيق

`.claude/hooks/docs-drift.sh` خطّاف `Stop` مسجَّل في `.claude/settings.json`، يعمل
تلقائياً عند انتهاء كل دور:

- **كودٌ معدَّل بلا توثيق معدَّل** (`server/`, `assets/`, `index.html`, `package.json`
  متغيّرة و`CLAUDE.md`/`README.md`/`docs/` ساكنة) → **يمنع إنهاء الدور مرة واحدة في
  الجلسة** ويطلب تشغيل `/sync-docs`. مرة واحدة فقط كي لا يصير حلقة لا تنتهي؛ ولو كان
  التغيير لا يستحق توثيقاً (تجربة عابرة) فقل ذلك واختم دورك.
- **آخر التزامٍ للكود أحدث من آخر تحديث لـ `CLAUDE.md`** → تنبيه للمستخدم بلا منع.

الطابع (`.claude/.docs-nudged`) يحمل معرّف الجلسة، ومُستبعد من git.
**لا تُعطّل هذا الخطّاف لتتخلّص من تنبيهه** — التنبيه هو الغرض: هذا الملف هو الذاكرة
الوحيدة بين الجلسات، وكودٌ يسبق توثيقَه يُنسى سببُه في الجلسة التالية.

## قرارات مهمة اتُّخذت وأسبابها

- **البطاقات أسلحة تُرمى على الخصم، لا قيوداً ذاتية**: طلب المستخدم صراحة عكس التصميم
  الأولي — `canPlayCard` ترفض الرمي في دورك (لا تستطيع رمي سلاح على نفسك)، ونقاط القيد
  تقع على **المتحدِّث المستهدَف** لا على الرامي، والقيد المرمي **ظاهر للطرفين** في
  `viewFor` — الهدف يجب أن يراه ليصارعه أثناء كلامه.
- **قضاة ثلاثة بشخصيات متضادة عمداً**: الميزان (عادل يغريه الشعر، الأصلي)، الريح
  (عشوائي متناقض، بأرضية دنيا صريحة ٠-٢ للصمت "كي تكون الفوضى ممتعة لا عقابية")، العُرف
  (شيخ قبَلي يكره الفلسفة ويكافئ الإيجاز ويعاقب الشعر كـ"زخرف" — **نقيض الميزان
  عمداً**). قرعة روليت بصوت (Web Audio بلا موسيقى) تختار القاضي قبل كل محاكمة.
- **سَنَد بلا وكيل ذكاء اصطناعي عمداً**: الحكم بين اللاعبين أنفسهما (خصم يكذّب/يصدّق)،
  فلا نداء ولا تكلفة ولا حاجة مايكروفون — قرار مستخدم صريح منذ بداية تصميم اللعبة.
  المكالمة بين اللاعبين **خارج التطبيق تماماً**.
- **`hint` أربع كلمات تسمّي الموضوع لا الدعوى**: بعد أن اكتُشف عبر مراجعة ذاتية أن
  تلميحات الخيارات "العبثية" الأولى كانت تفضح الكذب مباشرة (مثلاً تصف الحدث الخيالي
  نفسه)، فيتجنَّب المنصِت النقاش ويكسب أعلى فئة نقاط (٥) بلا مواجهة — أُعيدت كتابة ٣٥
  تلميحاً لتصف **موضوع** الرواية المحايد لا حقيقتها/كذبها. تحقّق آلي (طول ٤ كلمات + ليس
  اقتطاعاً حرفياً من النص) يعمل **عند إقلاع الخادم**، فيفشل الإقلاع فوراً بدل فشل صامت
  أثناء اللعب.
- **النقاط تطير من البطاقة ثم تُعدّ**، لا تظهر فجأة — طلب مستخدم صريح لتحسين الإحساس
  بالحركة، مع دعم كامل لـ `prefers-reduced-motion` (يتجاوز سلسلة الانميشن بالكامل، لا
  يكتفي بإخفائها بصرياً بـ CSS فقط).
- **مَعاني سباقٌ لا امتحان — وبلا مؤقّت**: اختار المستخدم صراحةً «سباق متزامن، الأسرع
  يكسب» و«لا يعتمد على الوقت يعتمد على الانتهاء من اللعبة، وكل مستوى أربعة أسئلة».
  فلا عدّاد تنازلي في أي مكان: الجولة تنتهي بالإجابة. والخطأ يكلّف **فرصةً** (خروجٌ من
  السؤال) لا وقتاً — فمن أخطأ لا ينتظر شيئاً، بل يترك الميدان لخصمه فوراً.
- **مستوى لغة مَعاني فصحى مألوفة لا غريب معجم** (قرار مستخدم صريح): التحدي في سرعة
  الالتقاط لا في حفظ المعاجم، ولو احتاج اللاعب معجماً ماتت متعة السباق.
- **نقاط مَعاني تتصاعد ١/٢/٣ مع المستوى**، فمن تأخّر في المقابلة يبقى له مطمع في الدخيل.
- **شبكة الحصاد تحمل الجواب وفخَّه معاً**: إن طُلب المرادف زُرع الضدّ والعكس — بلا هذا
  يصير الالتقاط بلا مقاومة. والحشو **أسماء أشياء محسوسة** عمداً (مِفتاح، قِنديل) وكلماتُ
  اللعبة معانٍ مجرّدة، فيستحيل أن يصير الحشو جواباً ثانياً لا أن يكون ذلك مأمولاً فقط.
- **دخيلةُ المستوى الثالث مناقِضةٌ للحقل لا شاردةٌ عنه** (شُحّ بين كلمات الكرم): فيُخطئ
  من ينظر إلى المجال وحده دون اتجاهه.
- **ترتيب شبكات مَعاني يُرصف في الخادم لا في المتصفح**: وإلا صار السباق بين رصفين
  مختلفين، فيسبق من وقع جوابه أعلى شاشته.
- **قيمة شخصية «مَن أنا» تتناقص مع التلميح، والخطأ يُقفل حتى التلميح التالي**
  (قراران صريحان للمستخدم): التناقص يخلق المعضلة التي تقوم عليها اللعبة، والقفل
  المؤقّت يجعل الخطأ يكلّف **فرصةً ووقتاً** بلا أن يُجلس صاحبه دقيقةً متفرجاً كما
  لو أُخرج من الشخصية كلها. **والتخمين الخاطئ يُعرض بنصّه للطرفين** (قرار صريح):
  متعةٌ اجتماعية ومعلومةٌ يكسبها الخصم، وقد فُضِّل على الحجب رغم أنه يكافئ المتأخر.
- **شخصيات «مَن أنا» عربية عامة لا أدبية فقط** (قرار صريح): أدباء وقادة وعلماء
  ورحّالة — مجمّعٌ أوسع فيتأخر التكرار، وتلميحاته أغنى تنوّعاً من مجمّع سَنَد الأدبي.
- **المزاد يدويٌّ عمداً بلا بنكٍ ولا وكيل** (قرار مستخدم صريح: «الفكرة أنها يدويه
  أكثر من أنها بنك أسئله ومعلومات»): الشرط يكتبانه، والوقت والنقاط يضبطانهما، والحكم
  بضغطةٍ منهما. فلا شيء في الخادم يعرف «الجواب» — والتطبيق لوحُ مزادٍ ومؤقّتٌ
  ومسجّل، والعقلُ عقلُهما. وهذا يقلب ما قامت عليه الأربع قبلها (خادمٌ يعرف ما لا
  يعرفه اللاعب)، ومنه جاء استثناء `viewFor` المتماثلة.
- **كلاهما يملك كل أدوات المزاد إلا دور المزايدة** (قرار صريح): صديقان يلعبان
  بمكالمةٍ بينهما يتفقان بالكلام، فقفلُ الأدوات على واحدٍ يجعل الآخر يستأذن في كل
  ضغطة. والمزايدة وحدها تناوبٌ بطبيعتها، ولولاه لم يكن مزاداً.
- **المتحدّي صاحبُ آخر مزايدة لا من سلّم** (قرار صريح): صاحبُ الدور يقول «عليك —
  هاتها» فتقع على خصمه بالرقم الذي زايده ذاك بنفسه. **وأتمّها ← له، عجز ← لخصمه** —
  فالمزايدة مقامرةٌ من الطرفين: تزيد فتُخيف خصمك، أو يسلّمها لك فتُجبَر على إثبات
  ما ادّعيت. ومن سلّم قد يربح بلا أن يتكلّم.
- **مايك المزاد مسجِّلٌ لا محلِّل**: كل مقطعٍ نهائي (`isFinal`) يصير شارةً — الوقفة
  بين اسمٍ واسمٍ هي الفاصل الطبيعي. ومعه حقلُ كتابة لمن لا يريد المايك أو أخطأ
  عليه التعرّف، **والشارة تُحذف بضغطة** لأن التعرّف الصوتي يخطئ حتماً.
- **الحذف يحمل نصّ الشارة مع موضعها**: الزرّان ✕ بيد اللاعبَين معاً، فلو ضغطا معاً
  على شارتين وصل الأمر الثاني بعد أن أزاح الأولُ المواضع، فحُذفت شارةٌ لم يقصدها
  أحد. الختمُ يرفض الحذف عندها ويعيد البثُّ القائمةَ الصحيحة.
- **وعاءٌ ثابت لكل لعبة في المباراة لا جمعُ نقاطها الخام** (قرار مستخدم صريح):
  سلالم الألعاب متباعدة جداً، فالجمع الخام يجعل لعبةً واحدة تبتلع المباراة
  ويُفقد بقيةَ الجولات معناها. وبالوعاء الثابت يستوي من سحق خصمه ٢٤ إلى صفر
  ومن فاز بفارق نقطة — لأن السؤال «كم لعبةً كسبت» لا «كم نقطةً جمعت».
- **جولات المباراة بأطوالها الكاملة بلا تقصير** (قرار صريح): لا نسخةً مختصرة من
  اللعبة داخل المباراة، فمباراةٌ من ثلاث ألعاب جلسةٌ طويلة مقصودة.
- **لوحة الجلسة «قبل اللعب وحده»** (قرار صريح) — و«قبل اللعب» يعني **قبل أن تبدأ
  الجلسة فعلاً** (`status !== 'lobby'`) لا قبل ظهور شاشة اللعبة: المُنشئ يدخل
  الميدان فوراً وينتظر خصمه فيه، فلو حُجب اللوح عن كل شاشات الألعاب لما رآه صاحبُ
  الدعوة أصلاً وهو أوّل من يحتاجه. والحاضرون **يُقرأون من الحالة لا يُخمَّنون**.
- **مفردات «مَعاني» أصعب بلا معجم** (قرار صريح يعدّل سابقَه): فصحى يعرفها المتعلّم
  من القرآن والشعر ولا يقولها في يومه (وَجَل، جَلَد، إذعان، ضَنك)، لا غريبَ معجمٍ
  يحتاج بحثاً (يَفَن، عَيطَل) — فلو احتاج اللاعب معجماً ماتت متعة السباق.
- **شخصيات «مَن أنا» مشهورةٌ لا نادرة** (قرار صريح): المعضلة فيها **متى تجازف**
  لا **هل تعرف**، وشخصيةٌ لا يعرفها أحد تقتل المعضلة — تنفد تلميحاتها بلا نقاطٍ
  لأحد. ولذلك وُسّع المجمّع إلى ١٩ بمشاهيرَ يعرفهم كل عربي.
- **HTTPS اختياري (`npm run https`) بشهادة ذاتية التوقيع** لأن المتصفح يمنع المايكروفون
  على أصل `http://` غير `localhost` — لازم عند اللعب بين جهازين على نفس الشبكة المحلية.
- **`.env` مخصَّص بلا حزمة `dotenv`** — تحميل يدوي بسيط في `setup.js`، لا يكتب فوق متغيرات
  بيئة موجودة مسبقاً.

## أشياء جُرِّبت ولم تنجح (لا تُكرَّر)

- **`requestAnimationFrame` لفتح الطبقات (overlays)** — لا يعمل في تبويب خلفي/غير
  مُركَّز، فتبقى شاشة اللاعب غير النشط شفافة. استُبدل بإعادة تدفّق قسرية في كل مكان
  (انظر قسم المعمارية أعلاه).
- **انتظار القاضي داخل `submitSpeech` بشكل متزامن (blocking)** — يجمّد اللعبة 5-15 ثانية
  في كل مرافعة (٤ مرات لكل محاكمة). استُبدل بنموذج غير حاجب (`submitSpeech` تتقدّم فوراً،
  `applyJudgement` تُلحق لاحقاً).
- **Aref Ruqaa لأي نصٍّ يُقرأ لا يُلمح** — بدأ محصوراً في العناوين ≥30px، ثم زحف إلى
  ٢٩ موضعاً (كلمات مَعاني بـ32px، أسماء شخصيات سَنَد، أزرار «ثقة/كذّاب» بـ25px، لوحات
  القضاة بـ18px، الأرقام الطائرة). **قال المستخدم صراحةً إنه صعب القراءة في كل
  الألعاب**، فسُحب إلى سبعة عناوين طبقات فقط. الرقعة جميلة لكنها تُبطئ الالتقاط —
  وفي لعبة سباق يكلّف ذلك نقاطاً، ومع التشكيل يزداد الازدحام. **لا تُعِد توسيعه.**
- **تلميحات سَنَد تصف الحدث العبثي نفسه** — جعلت الخيار الأعلى نقاطاً (٥) يُخمَّن فوراً
  بلا نقاش، فمات أعلى مستوى في اللعبة. أُعيدت الكتابة لتصف الموضوع لا الدعوى.
- **حذف قواعد CSS ميتة بعمليات استبدال نصية/تعبيرات نطاق (range-slice) بلا مراجعة
  دقيقة** — أدّى مرتين لحذف مفرط لملف `styles.css` بالكامل تقريباً (مرة نزل لنحو ١٣٠٠
  بايت فقط). عولج بـ `git checkout` + حذف كل قاعدة بتعبير دقيق منفصل. **عند حذف CSS، احذف
  قاعدة بقاعدة بمطابقة دقيقة، لا نطاقات سطور تقديرية.**
- **إدراج ترميز HTML جديد عبر استبدال نصي (Python string-replace) مرتكز على مسافات
  بادئة (whitespace) قد تكون انزاحت** — فشل صامتاً (لم يُدرَج الزر فعلياً) بينما استدعاء
  `querySelector` غير محروس بجانبه رمى استثناءً أوقف دالّة ربط الأحداث كاملة، فتعطّلت
  شاشة سَنَد بالكامل. **استخدم أداة `Edit` بمطابقة سياق فعلي من الملف، لا استبدالاً
  نصياً تخمينياً، عند تعديل HTML موجود.**
- **التنبيه (`window.alert`) على كل رسالة خطأ من الخادم في لعبة سباق** — ضغطُ الطرفين
  على «التالي» يجعل الثانية تصل بعد مغادرة مرحلة الكشف، فيُرفض رفضاً طبيعياً: ١١ تنبيهاً
  حاجباً في جلسة من ١٢ سؤالاً (قِيس فعلياً). عولج بوسم `soft:true` على رفض السباق.
- **طبقة مُعلِنة بلا `pointer-events:none` فوق لوحٍ حيّ** — انظر قاعدة الطبقات أعلاه.
- **صنف انميشن لا يُنزَع بعد انتهائه فوق عنصرٍ له انميشن دائم** — `.ma-emblem.dip`
  يغلب على `emblem-breathe` بالتخصيص، فتوقّف تنفّس الشعار عند أول تلميح للأبد.
  عولج بنزعه عند `animationend`. **كل صنف حركةٍ عارضة على عنصرٍ متحرّك دائماً
  يجب أن يُنزَع.**
- **مطابقة الجواب بتساوٍ تامّ في لعبة إجابةٍ كتابية** — «الشاعر المتنبي» و«أبو
  الطيب أحمد بن الحسين المتنبي» كانا يُحسبان خطأً فيُقفل صاحبهما. عولج بالاحتواء.
- **`.btn` في هذا المشروع عرضه `width:100%`**، فـ`flex:none` وحده لا يكبحه داخل
  صفٍّ — لزم `width:auto` صراحةً، وإلا ابتلع الزرُّ الصفَّ وسُحق حقل الإدخال.
- **صنفُ تنسيقٍ يُستعار من لعبةٍ أخرى** (`.mn-gain` في لوح حكم المزاد) — جرّ معه
  قاعدةَ `@media` غير المقصورة الخاصة بشبكة تلك اللعبة (`grid-column: 2`)، فكسر
  صفَّ الحكم على الجوال. **الصنف المشترك يلزمه قواعدُه مقصورةً على سياقها**
  (`.mn-verdict > .mn-gain`)، وإلا فصنفٌ خاص. عولج بالأمرين معاً.
- **رقعةُ إعدادٍ تُطبَّق حقلاً حقلاً ثم تُرفض عند أول فاسد** — بقي التعديل الأول في
  الخادم، والمناولةُ لا تبثّ عند الرفض، فرأى صاحبُه شرطاً لا يراه خصمه أبداً.
  **تحقّق كاملاً قبل أي تعديل**، والرقعةُ تُرفض كلُّها أو تُطبَّق كلُّها.
- **حرف `NUL` حرفيّاً في مصدر JavaScript** (فاصلاً في `join`) — يجعل `git diff`
  يقول «ملفات ثنائية تختلف» ويمتنع عن عرض التغيير، و`grep` لا يبحث في الملف أصلاً.
  استُبدل بـ `'\u0000'` هروباً: القيمة نفسها والملف يبقى نصّاً.
- **افتراض أن آلات الألعاب تتّفق على اسم حالة الانتهاء** — المحاكمة
  `session-over` والأربعُ `over`، ففحصٌ باسمٍ واحد جعل جولةَ محاكمةٍ لا تُحسم
  أبداً فتموت المباراة عندها. عولج بـ`gameEnded()` تقبل الاسمين.
- **حذفُ الغرفة لحظة خلوّها من المقابس** — تبديل جولة المباراة يُغلق مقبسَي
  الطرفين معاً، فكانت المباراة وأرصدتها تتبخّر بينهما. عولج بمهلة `REAP_MS`.
- **إطفاءُ مؤقّتات الغرفة عند خلوّها بلا إعادتها عند العودة** — عاد اللاعبان إلى
  «مَن أنا» وعدّادُها واقفٌ على صفرٍ إلى الأبد. عولج بإعادة الجدولة في `keepAlive`.
- **زرٌّ تحت طبقةٍ مُعلِنة** — زرّ «الجولة التالية» في شريط المباراة يقع تحت طبقة
  الختام فيبدو قابلاً للنقر وليس كذلك. **طريقٌ واحدٌ ظاهرٌ في كل لحظة لا طريقان**،
  والشريط يُعيد رسم نفسه بمراقبة سمة `hidden` على الطبقة.
- **`transitionend` وحده لإخفاء طبقةٍ بعد إغلاقها** — من قلّل الحركة لا انتقالَ
  عنده فلا حدث، فتبقى الطبقة (`inset:0`) شفافةً تبتلع كل نقرةٍ في الصفحة إلى
  الأبد. عولج بمؤقّتٍ صريح (صفر مع تقليل الحركة).
- **دوالُّ ربط الواجهة ليست خاملةَ التكرار** — إعادة الدخول إلى اللعبة نفسها بعد
  العودة للرئيسية كانت تُضاعف المستمعين، فضغطةٌ واحدة تزايد مرّتين أو تتخطّى
  جولة. عولج بحارس `bound` في `enterGame`.
- **رايةُ «عُرض الختام» لا تُصفَّر** — الجلسة الثانية على الصفحة نفسها (جولةُ
  مباراةٍ تالية) لا ختام لها، ولا ذيلَ مباراةٍ ولا زرَّ جولةٍ تالية معه. تُصفَّر
  الآن في `leave()`.
- **`el.dataset.x = ''` لإلغاء حالة يُنتقى عليها بـ `[data-x]`** — السمة تبقى موجودة
  بقيمة فارغة فيطابقها المحدِّد. الإلغاء الصحيح `delete el.dataset.x`.
- **مولّد إشارة الخادم يفترض `generate()` من `selfsigned` متزامناً** — فعلياً يُرجع
  Promise في النسخة المثبَّتة؛ عدم انتظاره أدّى لشهادة فارغة/تعطُّل صامت. أُصلح بـ `await`.

## أخطاء حرجة وقعت وأُصلحت (سياق تاريخي، ليست أخطاء حالية)

كل ما يلي **أُصلح بالكامل ولا يمثّل مشكلة قائمة** — مذكور فقط لتجنُّب تكراره أو الشك في
الكود الحالي:

- خادم ملفات بلا قائمة بيضاء كان يُقدِّم `.git/config`/`.env` لأي زائر → `resolvePublic()`.
- بلا `room.busy` كانت ضغطتان تبدآن محاكمتين متزامنتين. بلا حارس رسائل لكل لعبة
  كانت رسالة محاكمة ضالّة تُدمِّر حالة غرفة سَنَد وتستدعي القاضي بلا داعٍ.
  → أُضيف الحارسان معاً، ويجب أن يبقيا معاً.
- فشل نداء القاضي كان يترك الغرفة عالقة بلا بثّ حالة جديدة → كل معالج `catch` الآن يعيد
  `room.busy=false` + `broadcast()` + رسالة خطأ.
- فشل نداء الاعتراض كان يُحسَم تلقائياً كـ"مرفوض" فيُعاقَب اللاعب على عطل تقني → عند
  الفشل الآن: يُلغى الاعتراض المعلَّق بلا تسجيل نقاط، **وتُعاد البطاقة المستهلَكة**.
- لاعب منقطع الاتصال كان يُمنَع من العودة رغم علم الخادم بذلك → منطق استرجاع مقعد شاغر
  عند `join`.
- عدم وجود حدّ معدَّل (rate limit) كان يسمح بإغراق نداءات القاضي المدفوعة → `RATE` لكل
  مقبس + حدّ حجم رسالة ٨١٩٢ بايت.
- بحث `.find(o=>o.kind==='true').text` بلا حراسة في `viewFor` كان يرمي استثناءً ويجمّد
  الغرفة إن كانت أي شخصية ناقصة خيار `true` → حراسة `?.` + سقوط آمن + تحقّق وقت-الإقلاع.
- رمز الفوز بأنيميشن كان يقفز نصف حجمه (`transform` مضمَّن يبطل `translate(-50%,-50%)`
  الأساسي) → دُمج بـ `calc(-50% + Npx)`.
- أول عرض بعد إعادة الاتصال كان يرسم كل النقاط التراكمية كأنها نقاط الجولة (لا فحص
  `hasOwnProperty` على قيمة أولية) → فحص صريح يرسم القيمة مباشرة بلا انميشن أول مرة.
- مؤقّتات انميشن نقاط غير مُلغاة كانت تجعل الرقم يقفز للخلف ويُعاد عدّه عند بثّ حالة
  جديدة أثناء الانميشن، **ثم** نفس الإصلاح كسر انميشن ختام اللعبة (`finale`) بنفس الآلية
  → `countUp()` أخذت وسيط `cancellable` (افتراضي `true`، `false` لنداءي الختام فقط).
- طبقة `#finale` بلا زر إغلاق كانت تُسقط كل النقر في الصفحة للأبد بعد انتهاء الجلسة →
  زر `#finale-close` مضاف.

## القيود/القواعد التي يجب الالتزام بها دائماً (ملخّص تنفيذي)

- المفتاح والبطاقات والدرجات لا تغادر الخادم — المتصفح يرى `viewFor` فقط.
- `resolvePublic` قائمة بيضاء لا سوداء.
- `room.busy` يحرس كل نداء للقاضي وكل بدء محاكمة/جلسة.
- أي فشل نداء قاضٍ يُعيد بثّ الحالة، لا يترك اللاعب عالقاً.
- نص شخصية القاضي ثابت البايتات ولا يغادر الخادم (كسر هذا يُبطل التخزين المؤقت صامتاً).
- `REVEAL_MS` أطول من مدة انميشن القرعة.
- الطبقات تُفتح بإعادة تدفّق قسرية لا `requestAnimationFrame`.
- `submitSpeech` لا تنتظر القاضي.
- نص/نوع/نقاط رواية سَنَد لا تغادر الخادم للخصم إلا `hint` (٤ كلمات، تسمّي الموضوع لا الدعوى).
- الغرفة تحمل لعبتها؛ `GAME_MESSAGES` يحرس كل رسالة، وكل لعبة جديدة تُسجَّل فيه.
- جواب سؤال مَعاني وتعليله لا يغادران الخادم قبل الكشف، ولا اختيارُ الخصم.
- اسم شخصية «مَن أنا» وصيغه وكشفه لا تغادر الخادم قبل الحلّ، ولا تلميحٌ لم يحن وقته.
- مؤقّت «مَن أنا» يُلغى عند الحلّ وعند إغلاق الغرفة، وتُجدوَل دورته قبل البثّ لا بعده.
- جرس «المزاد» يُلغى عند إغلاق الغرفة، ويُجمَّد العدّاد قبل بثّه، ولا يُنهي الجولة.
- `viewFor` المتماثلة للمزاد وحده — وشرطُها أن الخادم لا يعرف شيئاً لا يعرفه اللاعبان.
- رفضُ المزاد `soft` لكنه يُشرَح في `#mz-note` — لا ضغطةَ تُبتلع بلا خبر.
- `room.game` لعبةُ الجولة الجارية؛ حارس الرسائل يتحرّك معها ولا يُضعَّف.
- `gameEnded()` تقبل `over` و`session-over` معاً — أي لعبةٍ جديدة تُفحَص بها.
- `settleRound` في `broadcast` وحده، و`recordRound` لا تصرف وعاء جولةٍ مرّتين.
- غرفةٌ خلت تُمهَل (`REAP_MS`) ولا تُحذف فوراً، ومؤقّتاتها تُعاد عند العودة.
- العودة بمعرّف اللاعب لا بالتقاط أيّ مقعدٍ شاغر.
- طريقٌ واحدٌ ظاهرٌ لكل إجراء في كل لحظة — لا زرّاً تحت طبقةٍ تحجب نقره.
- كل رواية `true` تُراجَع صحّتها التاريخية قبل الإضافة.
- بلا ملفات صوت/موسيقى — Web Audio مولَّد فقط.

## حالة الاختبارات والتحقق

`npm test` حالياً **136/136 ناجحة** في تسعة ملفات (`state`, `judges`, `sanad-state`,
`sanad`, `maani`, `man-ana`, `mazad`, `match`, `e2e`). `e2e.test.js` يشغّل عملية خادم حقيقية
(`REVEAL_MS=0`, `CLUE_MS=400`, `ANTHROPIC_API_KEY=''` للسرعة والحتمية) ويتصل بعملاء
WebSocket حقيقيين — يغطي تدفّق المحاكمة الكامل، تدفّق سَنَد الكامل، سباق مَعاني
الكامل، دورة تلميح «مَن أنا» التي يقودها الخادم (تقدّم التلميح وتناقص القيمة ووقف
المؤقّت عند الحلّ ونفاد التلميحات)، دورة «المزاد» الكاملة في خمسة اختبارات (مزايدة
من غير صاحب الدور تُرفض **ويصل سببُها**، «عليك هاتها» والمتحدّي صاحبُ آخر مزايدة،
الإجابات من الطرفين وختمُ الحذف، الحكم بالوجهين، الجولة التالية وإنهاء الجلسة)،
عزل الرسائل بين الألعاب الخمس (بكل الاتجاهات)، رفض الانضمام للعبة خاطئة، توقيت
التقييم غير الحاجب (<1500ms)، وانتظار الحكم النهائي.

و**المباراة في سبعة اختبارات من الطرف للطرف**: الوعاء الثابت وتبديل الجولة
للطرفين معاً، وانقلابُ حارس الرسائل مع الجولة (رسالةُ لعبةٍ لم يحن دورها تُرفض،
ثم تنقلب الآية بعد التبديل)، ومنعُ التقدّم قبل حسم الجولة، ولعبةٌ مفردة بلا
لقطةِ مباراة، والانضمامُ بأيّ لعبةٍ من جولاتها، و**حسمُ جولة محاكمةٍ رغم أن
حالتها تُسمّى `session-over`**، و**انقطاعُ الطرفين معاً**: الغرفة لا تُبخَّر،
وكلٌّ يرجع إلى مقعده بمعرّفه، والمؤقّت يُستأنف فيتقدّم التلميح بعد العودة.

`maani.test.js` و`man-ana.test.js` يبنيان الجلسة بأسئلة/شخصيات معلومة
(`startSession(s, () => deck)`) فلا يختبران عشوائيةً — والعشوائي (`buildDeck`) يُختبر
وحده على ٢٠ دورة. و`man-ana.test.js` يستدعي `advanceClue` مباشرةً فلا ينتظر ثانيةً.
و`mazad.test.js` (١٨ اختباراً) يمرّر `now` لكل دالّة مؤقّت، فتمضي «دقيقة» بسطرٍ واحد.
و`match.test.js` (١١ اختباراً) يختبر الطبقة وحدها بنتائج مصطنعة، فلا يشغّل لعبة.

بعد أي تعديل على منطق أو واجهة: شغّل `npm test`، ثم تحقّق بصرياً عبر `/run` (Playwright)
— **الاختبارات الآلية لا تكتشف باگات بصرية/تفاعلية** (مثل مشكلة `requestAnimationFrame`
بالتبويب الخلفي، التي اكتُشفت فقط عبر لقطات شاشة فعلية)، ثم `/code-review` قبل الدفع.

## بنود مفتوحة / TODO (ليست أخطاء — نطاق غير مبني بعد أو غير مُتحقَّق منه)

- **القاضي الحقيقي لم يُختبر بمفتاح API فعلي إطلاقاً** في بيئة التطوير هذه — جودة أحكامه
  الفعلية (لا الوهمية) غير معروفة. إن توفَّر مفتاح لاحقاً، اختبر خاصةً: التزام النموذج
  بمخطط Zod، جودة عبثية القضايا المولَّدة، اتساق شخصية كل قاضٍ الثلاثة.
- **دقّة Web Speech API على اللهجة النجدية/السعودية غير مقاسة** — قد تحتاج تعليمات نطق
  أو معالجة نص إضافية.
- **مؤقّت المحاكمة من جهة العميل فقط** (client-side) — نموذج ثقة بلا تحقّق خادم؛ يحتاج
  نقلاً لجهة الخادم إن أُريد لعب بين طرفين غير موثوقين.
- **استرجاع المقعد عند إعادة الاتصال مبسَّط وقائم على الثقة** — لا حماية من انتحال هوية
  لاعب منقطع.
- **لا بث تدريجي (streaming) لتعليق القاضي** حرفاً بحرف — يظهر التقييم دفعة واحدة.
- **البنوك وُسّعت ولم تُحَلّ مشكلةُ التكرار**: سَنَد ١٧ شخصية (تُنتقى ٤)، ومَن أنا
  ١٩ (تُنتقى ٣)، ومَعاني ٥٤+٣٤+٢٨ (تُنتقى ٤+٤+٤). التكرار تأخّر إلى نحو أربع أو
  خمس جلسات بدل جلستين، لكن **لا ذاكرة عبر الجلسات** بعد — وهي الحلّ الحقيقي.
- **لا تصعيد داخل مستوى مَعاني الواحد**: أسئلته الأربعة متساوية الصعوبة، والبنك
  الآن فيه طبقتان (مألوفة وأصعب) تُخلطان بلا ترتيب.
- **سباق مَعاني يكافئ سرعة الشبكة** عند التساوي — مقبول بين صديقين، غير عادل بين
  طرفين متباعدين.
- **تكرار مؤكَّد بين ملفات الواجهة الأربعة** `sanad.js` و`maani.js` و`man-ana.js`
  و`mazad.js` (الصوت، لوح النقاط، الختام) — التجريد صار مستحقاً ومتأخراً بعد خمسة
  أمثلة ولم يُنفَّذ بعد، انظر «بناء لعبة سادسة» أعلاه.
- **نساء «مَن أنا» أربعٌ من ١٩** (الخنساء، شجرة الدر، رابعة العدوية، زرقاء
  اليمامة) — تحسّنت عن واحدةٍ من عشر ولم تتوازن بعد. وسَنَد فيها امرأتان من ١٧.
- **صيغ جواب «مَن أنا» مؤلَّفة يدوياً**: اسمٌ مشهورٌ نسيه المؤلّف يُرفض جوابه الصحيح
  ويقفل صاحبه ١٥ ثانية. الاحتواء خفّف ذلك ولم يُلغه.
- **سجلّ جولات «المزاد» لا يُعرَض**: `s.log` يُجمَع في الخادم ولا يصل الواجهة، فلا
  يستطيعان مراجعة شرطٍ قديمٍ أو من كسبه.
- **إجابات «المزاد» لا تُقارَن ببعضها**: تكرار الاسم مرتين يُحسب شارتين والعدّ لا
  ينتبه — الحذف اليدوي هو العلاج، وهو مقبولٌ في لعبةٍ يدويّة بين صديقين.
- **«المزاد» يفترض حسن النيّة**: لا حكم آليّ ولا تحقّق من الإجابات — وهذا تصميمٌ
  لا نقص، لكنه يعني أن الجلسة تقوم على اتفاقهما لا على الخادم.
- **لا انسحاب من جولة مباراةٍ جارية**: لا سبيل لتخطّي لعبةٍ بدأت إلا بإنهائها أو
  بالخروج من الديوان كلّه؛ وترتيبُ الجولات ثابتٌ بعد البدء.
- **وعاء المباراة لا يُظهر فارق النقاط داخل اللعبة**: مقصودٌ، لكنه يُخفي معلومةً
  قد تُهِمّ اللاعبَين.
- **مباراةٌ من ٣ ألعابٍ بأطوالها الكاملة تتجاوز ٢٥ دقيقة** (والمحاكمة وحدها ~١٥):
  قرارٌ صريح للمستخدم، لكنه يعني أن المباراة الطويلة جلسةُ سهرة لا لعبةً عابرة.
- **لوحة الجلسة لا تُرى أثناء اللعب**: قرارٌ صريح، فمن أراد الرمز وسط الجولة
  يجده في رأس الشاشة لا في اللوح.
- **٦ من ١١ لعبة في الكتالوج بطاقات عرض فقط** بلا شاشة ولا منطق ولا وكيل (`qafiyat-attayeh`,
  `alkalima-almuharrama`, `marthiyat-ashshahin`, `sayf-wa-qalam`, `tafsir-attalasim`,
  `madh-alkhasm`) — أسماؤها وأوصافها مبدئية غير معتمدة، تُبنى لاحقاً لعبة لعبة (ليس معاً)
  بنسخ نمط `rules.js`/`state.js` كما في المحاكمة وسَنَد.

## آخر ما جرى العمل عليه، وحالته الحالية

آخر ميزة مكتملة: **مرونة المنصّة — المباراة ولوحة الجلسة وتوسعة البنوك**:

1. **طبقة المباراة** (`server/match.js` + `docs/match.md`): عدّة ألعابٍ جولاتٍ في
   ديوانٍ واحد بوعاءٍ ثابت، و`room.game` صار **لعبة الجولة** فيتحرّك حارس العزل
   معها بلا إضعاف. ومعها مهلةُ إمهالٍ للغرف (`REAP_MS`) وعودةٌ بمعرّف اللاعب.
2. **لوحة الجلسة**: زرٌّ جانبيّ يفتح لوحاً فيه الحاضرون (من الحالة لا من التخمين)
   والرمزُ ورابطُ الدعوة وزرُّ الخروج للرئيسية — يختفي حين تبدأ الجلسة فعلاً.
3. **العودة للرئيسية** من ذيل الختام ومن شريط المباراة ومن اللوح.
4. **الاختيار التراكمي** في شاشة الألعاب مع ترقيم الجولات، وبطاقاتُ العرض غير
   المبنيّة تُفرِد نفسها بتنبيهٍ صريح.
5. **توسعة البنوك الثلاثة**: مَعاني ٥٤ زوجاً و٣٤ حصاداً و٢٨ دخيلاً و٦٤ حشواً
   (بطبقةٍ أصعبَ بلا معجم)، ومَن أنا ١٩ شخصية مشهورة (النساء ٤ بدل ١)، وسَنَد
   ١٧ شخصية و١٥٣ خياراً.
6. **`assets/app.js` أُعيد بناؤه**: خمسُ دوالّ `enterX` متكرّرة صارت `enterGame`
   واحدة على خريطة `GAME_UI`.

جرت جولة `/code-review` وأُصلحت **ملاحظاتها الإحدى عشرة كلها**، وثلاثٌ منها كانت
تُميت المباراة: اسمُ حالة انتهاء المحاكمة (`session-over`) لا يُفحَص، وزرُّ الجولة
التالية داخل طبقةٍ لا تفتحها المحاكمة أصلاً، ومؤقّتاتٌ تُطفأ عند خلوّ الغرفة ولا
تُعاد. وأُضيف من التحقّق البصري: زرُّ الشريط كان يقع تحت طبقة الختام فيبدو قابلاً
للنقر وليس كذلك.

و`npm test` أخضر **١٣٦/١٣٦** (منها ١١ لطبقة المباراة و٧ لها من الطرف للطرف)،
وتُحقِّق بصرياً بجلسات Playwright بلاعبَين: الاختيار المتعدّد، وتبديل الجولة عند
الطرفين معاً، وإغلاق اللوح والطبقات، والعودة للرئيسية — بلا أخطاء صفحة ولا تنبيهات.

**الخطوة القادمة المقترحة** (لا قرار محسوم): إما (أ) **ذاكرة عبر الجلسات** لتفادي
تكرار الشخصيات والكلمات — فالتوسعة أخّرت التكرار ولم تمنعه، أو (ب) استخراج
المشترك بين ملفات الواجهة الأربعة إلى `assets/duel.js` (التجريد متأخر منذ خمسة
أمثلة، وطبقةُ المباراة زادت المشترك)، أو (ج) بناء لعبة سادسة، أو (د) تفعيل
واختبار القاضي الحقيقي بمفتاح API إن توفَّر. اسأل المستخدم عن الأولوية إن لم
يحدِّدها.

## الفرع وسير العمل

التطوير على `claude/diwan-challenge-game-design-fq77kt`. **رسائل الالتزام بالعربية.**
لا تُنشئ Pull Request إلا إن طُلب صراحة. لا تلتزم (commit) إلا إن طُلب صراحة —
إلا أن استدعاء `/sync-docs` هو نفسه طلبُ الالتزام والدفع، فهو غرضُ الأمر.

دورة الميزة الواحدة، كما جرت في هذا المشروع كله:

بناء → `npm test` → تحقّق بصري بـ `/run` (Playwright، بلاعبَين) → `/code-review`
وإصلاح **كل** ملاحظة → `/sync-docs` → دفع.
